import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.tt.user.metrotrackercdmx',
  appName: 'Metro Tracker CDMX',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
